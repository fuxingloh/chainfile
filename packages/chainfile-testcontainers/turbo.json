{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "test": {
      "inputs": ["./src/**"],
      "dependsOn": ["@chainfile/agent#build:docker", "^build"]
    },
    "build": {
      "inputs": ["./src/**"],
      "outputs": ["./dist"],
      "dependsOn": ["@chainfile/agent#build:docker", "^build"]
    }
  }
}
