{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["Dockerfile", "hardhat.config.js", "hardhat.package.json"],
      "dependsOn": ["^build"]
    }
  }
}
